# إعدادات Cloudflare Flexible SSL - إضافة هذه للملف .env

# ⚠️ مهم: مع Flexible SSL لا تستخدم SECURE cookies
# لأن الاتصال بين Cloudflare والسيرفر يكون HTTP

# تأكد من أن APP_URL يستخدم HTTPS (للمستخدمين)
APP_URL=https://yourdomain.com

# إعدادات إجبار HTTPS (للـ URLs المولدة)
FORCE_HTTPS=true
ASSET_URL=https://yourdomain.com

# إعدادات Session للـ Flexible SSL
SESSION_SECURE_COOKIE=false
SESSION_SAME_SITE=lax

# إعدادات CSRF للـ Flexible SSL  
CSRF_COOKIE_SECURE=false
CSRF_COOKIE_SAME_SITE=lax

# Trust Proxies
TRUST_PROXIES=*

# Cloudflare settings
CLOUDFLARE_ENABLED=true
CLOUDFLARE_SSL_MODE=flexible

# إعدادات إضافية للـ Flexible SSL
MIX_HTTPS_ENABLED=true
